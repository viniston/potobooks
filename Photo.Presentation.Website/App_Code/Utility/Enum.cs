namespace Utility
{
    public enum Target { Development = 0, Staging = 1, Production = 2 }

	public enum MessageType { Error = 0, Warning = 1, Information = 2, Confirmation = 3 }

    public enum SEMMode { FlightsFrom, FlightsTo, Flights, FlightsOnLocations, FlightsOnAirline, Products, Product, FlightsDefault, ProductsDefault }
}
